<%= form_with(model: comic_book, local: true) do |form| %>
  <% if comic_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comic_book.errors.count, "error") %> prohibited this comic_book from being saved:</h2>

      <ul>
      <% comic_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
